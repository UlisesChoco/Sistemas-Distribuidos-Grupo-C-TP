<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/userStyles.css">
    <title>Ofertas de Donaciones</title>
</head>
<body>
    <%- include('../templates/navbarDonationRequests', { roles: roles }) %>

    <section>
        <div class="container">
            <h1>Ofertas de Donaciones</h1>
            <p>Aquí se listan las donaciones que otras organizaciones de la red han puesto a disposición.</p>

            <a href="/donation-offers/create" style="margin-bottom: 20px; display: inline-block;">+ Ofrecer una Nueva Donación</a>
            <table id="offers-table">
                <thead>
                    <tr>
                        <th>ID Oferta</th>
                        <th>ID ONG Donante</th>
                        <th>Categoría</th>
                        <th>Descripción</th>
                        <th>Cantidad</th>
                    </tr>
                </thead>
                <tbody id="offers-table-body">
                    <tr>
                        <td colspan="5" style="text-align: center;">Cargando ofertas...</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </section>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            fetch('/donation-offers/api/list')
                .then(response => response.json())
                .then(data => {
                    const tableBody = document.getElementById('offers-table-body');
                    tableBody.innerHTML = '';

                    if (!data || data.length === 0) {
                        tableBody.innerHTML = '<tr><td colspan="5" style="text-align: center;">No hay ofertas disponibles.</td></tr>';
                        return;
                    }

                    data.forEach(offer => {
                        offer.Lista_donaciones_ofrecidas.forEach(donation => {
                            const row = `
                                <tr>
                                    <td>${offer.ID_oferta}</td>
                                    <td>${offer.ID_organizacion_donante}</td>
                                    <td>${donation.Categoria}</td>
                                    <td>${donation.Descripcion}</td>
                                    <td>${donation.Cantidad}</td>
                                </tr>
                            `;
                            tableBody.innerHTML += row;
                        });
                    });
                })
                .catch(error => {
                    console.error('Error al cargar las ofertas:', error);
                    const tableBody = document.getElementById('offers-table-body');
                    tableBody.innerHTML = '<tr><td colspan="5" style="text-align: center; color: red;">Error al cargar las ofertas.</td></tr>';
                });
        });
    </script>

    <%- include('../templates/footer') %>
</body>
</html>